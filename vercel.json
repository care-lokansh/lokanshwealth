{
  "installCommand": "npm install && npm install --prefix webapp && npm install --prefix backend",
  "buildCommand": "npx prisma generate --schema backend/prisma/schema.prisma && npm run build --prefix webapp",
  "outputDirectory": "webapp/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "node_modules/.prisma/**,backend/node_modules/.prisma/**,node_modules/@prisma/client/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
